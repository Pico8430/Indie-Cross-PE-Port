function onCreate()
    makeLuaText('watermark', "Currently on the Beta Cuphead Build, things might not work as expected!", 0, 2, 700); -- You can edit the created by Prisma Text just dont change anything else
        setTextSize('watermark', 20);
        addLuaText('watermark');
    end